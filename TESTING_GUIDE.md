# 🧪 دليل اختبار التحسينات - Accura Delivery App

## 📋 نظرة عامة

هذا الدليل يوضح كيفية اختبار جميع التحسينات المطبقة في المشروع.

---

## ✅ قائمة الاختبار السريعة

- [ ] 1. Real-time Subscriptions (السائق)
- [ ] 2. Real-time Subscriptions (التاجر)
- [ ] 3. Real-time Subscriptions (العميل)
- [ ] 4. Row Level Security (RLS)
- [ ] 5. فحص صورة السائق
- [ ] 6. حساب الإيرادات الصحيح
- [ ] 7. Error Handling
- [ ] 8. تحديد الموقع GPS

---

## 🔴 الاختبار 1: Real-time Subscriptions للسائق

### الهدف:
التحقق من أن السائق يتلقى إشعارات فورية عند توفر طلبات جديدة.

### الخطوات:

#### 1️⃣ **الإعداد:**
```bash
# افتح التطبيق كسائق
1. سجل الدخول كسائق
2. افتح شاشة الطلبات المتاحة (driver-tabs/index)
3. تأكد من تفعيل "متاح" (Online)
```

#### 2️⃣ **السيناريو الاختباري:**
```typescript
// من جهاز آخر أو حساب عميل:
1. أنشئ طلب جديد من أي متجر
2. تأكد من أن حالة الطلب = "pending"

// على جهاز السائق:
✅ يجب أن يظهر Alert تلقائياً: "📦 طلب جديد متاح"
✅ يجب تحديث قائمة الطلبات تلقائياً
✅ يجب تشغيل صوت الإشعار
```

#### 3️⃣ **التحقق من Console:**
```javascript
// افتح Developer Tools وابحث عن:
🔔 [driver] Real-time order event: INSERT
✅ Auto-refresh: fetching new orders...
```

#### 4️⃣ **الاختبار المتقدم:**
```typescript
// قم بقبول الطلب من السائق، ثم:
// من حساب التاجر: حدّث حالة الطلب إلى "preparing"

// على جهاز السائق:
✅ يجب تحديث إحصائيات اليوم (Daily Stats)
✅ لا يجب ظهور Alert (لأنه تحديث وليس طلب جديد)
```

### ❌ الأخطاء المحتملة:
```
- "Subscription status: TIMED_OUT" 
  → تحقق من إعدادات Realtime في Supabase

- "No permission to listen to changes"
  → تحقق من RLS policies

- لا يظهر إشعار
  → تحقق من أن user?.id موجود
```

---

## 🏪 الاختبار 2: Real-time Subscriptions للتاجر

### الهدف:
التحقق من أن التاجر يتلقى إشعارات فورية لطلبات متجره.

### الخطوات:

#### 1️⃣ **الإعداد:**
```bash
1. سجل الدخول كتاجر
2. افتح شاشة الطلبات (merchant-tabs/orders)
3. تأكد من اختيار متجر نشط
```

#### 2️⃣ **السيناريو الاختباري:**
```typescript
// من حساب عميل:
1. أضف منتجات من متجر التاجر إلى السلة
2. أكمل عملية الطلب
3. انتظر قليلاً...

// على جهاز التاجر:
✅ يجب ظهور Alert: "🔔 طلب جديد!"
✅ عرض رقم الطلب والمبلغ الإجمالي
✅ تحديث قائمة الطلبات تلقائياً
✅ تشغيل صوت الإشعار
```

#### 3️⃣ **اختبار تحديث الطلب:**
```typescript
// على جهاز التاجر:
1. قم بتحديث حالة الطلب من "pending" إلى "accepted"

// يجب أن:
✅ يتم التحديث بنجاح
✅ لا يظهر Alert (لأنه أنت من قام بالتحديث)

// على جهاز العميل:
✅ يجب أن يرى التحديث فوراً
```

#### 4️⃣ **التحقق من Console:**
```javascript
🏪 [Merchant] Order event: INSERT {...}
🏪 [Merchant] Order event: UPDATE {...}
```

### 💡 نصيحة:
```typescript
// إذا كان لديك عدة متاجر:
- جرب التبديل بين المتاجر
- تأكد من أن الإشعارات تظهر للمتجر الصحيح فقط
```

---

## 👤 الاختبار 3: Real-time Subscriptions للعميل

### الهدف:
التحقق من أن العميل يتلقى تحديثات فورية عن حالة طلبه.

### الخطوات:

#### 1️⃣ **الإعداد:**
```bash
1. سجل الدخول كعميل
2. أنشئ طلب جديد
3. افتح شاشة طلباتي (tabs/orders)
```

#### 2️⃣ **السيناريو الاختباري:**
```typescript
// من حساب التاجر:
1. افتح نفس الطلب
2. حدّث حالته من "pending" → "accepted"

// على جهاز العميل:
✅ يجب ظهور Alert: "📦 تحديث الطلب"
✅ رسالة: "تم تحديث حالة طلبك #XXX"
✅ تحديث البيانات تلقائياً
✅ تشغيل صوت الإشعار
```

#### 3️⃣ **اختبار تدفق كامل:**
```typescript
// سيناريو واقعي:
pending → accepted → preparing → ready → picked_up → delivered

// في كل مرحلة على جهاز العميل:
✅ يجب ظهور إشعار
✅ تحديث Badge الحالة
✅ تحديث التبويب (Active/Completed)
```

#### 4️⃣ **التحقق من Console:**
```javascript
👤 [Customer] Order status changed: accepted
👤 [Customer] Order status changed: preparing
```

---

## 🔒 الاختبار 4: Row Level Security (RLS)

### الهدف:
التحقق من أن كل مستخدم يرى بياناته فقط.

### السيناريوهات الاختبارية:

#### 🧪 اختبار 4.1: عزل طلبات العملاء
```sql
-- سجل الدخول كعميل A
-- حاول الوصول لطلب عميل B:

-- في Console Developer Tools → Network → Supabase Request:
-- يجب أن ترى: 0 results (أو empty array)
-- وليس permission error (RLS يخفي البيانات تلقائياً)
```

```typescript
// اختبار عملي:
1. سجل الدخول كعميل A
2. افتح شاشة الطلبات
3. سجل خروج وسجل دخول كعميل B
4. افتح شاشة الطلبات

✅ العميل A يرى طلباته فقط
✅ العميل B يرى طلباته فقط
✅ لا تداخل بين البيانات
```

#### 🧪 اختبار 4.2: عزل منتجات التجار
```typescript
// سجل الدخول كتاجر A
1. افتح شاشة المنتجات
2. حاول تعديل منتج

// سجل الدخول كتاجر B
1. افتح شاشة المنتجات

✅ التاجر A يرى منتجاته فقط
✅ التاجر B لا يستطيع رؤية أو تعديل منتجات التاجر A
```

#### 🧪 اختبار 4.3: محاولة هجوم SQL
```javascript
// في Supabase Client:
const { data, error } = await supabase
  .from('orders')
  .select('*')
  .eq('customer_id', 'OTHER_USER_ID'); // محاولة الوصول لبيانات مستخدم آخر

// النتيجة المتوقعة:
✅ data = [] (فارغ)
✅ لا يوجد error (RLS يخفي البيانات)
```

#### 🧪 اختبار 4.4: التحقق من الفهارس
```sql
-- في Supabase SQL Editor:
EXPLAIN ANALYZE 
SELECT * FROM orders 
WHERE customer_id = auth.uid()
AND status = 'pending';

-- يجب أن ترى:
✅ Index Scan using idx_orders_customer_id
✅ Index Scan using idx_orders_status
-- وليس Seq Scan (بطيء)
```

---

## 📸 الاختبار 5: فحص صورة السائق

### الهدف:
التحقق من أن السائق لا يستطيع قبول طلبات بدون صورة شخصية.

### الخطوات:

#### 1️⃣ **الإعداد:**
```bash
1. سجل الدخول كسائق جديد (بدون صورة)
2. افتح شاشة الطلبات المتاحة
```

#### 2️⃣ **السيناريو الاختباري:**
```typescript
// حاول قبول طلب:
1. اضغط على زر "قبول الطلب"

// يجب أن يظهر:
✅ Alert: "الصورة مطلوبة"
✅ رسالة: "يرجى إضافة صورة شخصية قبل قبول الطلبات"
✅ زر: "فتح الإعدادات"
❌ لا يتم قبول الطلب
```

#### 3️⃣ **اختبار بعد رفع الصورة:**
```typescript
1. افتح الملف الشخصي (Profile)
2. ارفع صورة (من المعرض أو الكاميرا)
3. ارجع لشاشة الطلبات
4. حاول قبول طلب مرة أخرى

✅ يجب أن يسمح لك بقبول الطلب الآن
```

#### 4️⃣ **التحقق من قاعدة البيانات:**
```sql
-- في Supabase SQL Editor:
SELECT 
  p.id,
  p.avatar_url,
  dp.photo_url
FROM profiles p
LEFT JOIN driver_profiles dp ON p.id = dp.id
WHERE p.id = 'DRIVER_USER_ID';

-- يجب أن يكون أحدهما NOT NULL
```

---

## 💰 الاختبار 6: حساب الإيرادات الصحيح

### الهدف:
التحقق من أن حساب الإيرادات يستخدم `customer_total` وليس `total`.

### الخطوات:

#### 1️⃣ **إنشاء بيانات اختبار:**
```sql
-- في Supabase SQL Editor:
-- افترض أن لدينا طلب بالقيم التالية:
-- product_total = 100
-- delivery_fee = 20
-- service_fee = 10
-- tax_amount = 13
-- customer_total = 143 (الإجمالي الصحيح)
-- total = 120 (قيمة قديمة/خاطئة)
```

#### 2️⃣ **السيناريو الاختباري:**
```typescript
// سجل الدخول كتاجر
1. افتح شاشة الإحصائيات (Analytics)
2. لاحظ قيمة "إجمالي الإيرادات"

// يجب أن تستخدم:
✅ customer_total = 143 ريال
❌ ليس total = 120 ريال
```

#### 3️⃣ **التحقق من الكود:**
```typescript
// في app/(merchant-tabs)/analytics.tsx:
// يجب أن ترى هذا الكود:

const totalRevenue = completedOrders.reduce((sum, order) => {
  const amount = order.customer_total ?? order.total ?? 0;
  //             ^^^^^^^^^^^^^^^^^^^^  الأولوية الصحيحة
  return sum + (parseFloat(amount.toString()) || 0);
}, 0);
```

#### 4️⃣ **اختبار مع طلبات متعددة:**
```typescript
// أنشئ 3 طلبات:
Order 1: customer_total = 100, total = 90
Order 2: customer_total = 150, total = 140
Order 3: customer_total = 200, total = 180

// الإيرادات المتوقعة:
✅ 450 ريال (100 + 150 + 200) - صحيح
❌ 410 ريال (90 + 140 + 180) - خاطئ
```

---

## 🚨 الاختبار 7: Error Handling

### الهدف:
التحقق من أن الأخطاء تُعالج بشكل صحيح وتظهر رسائل مفيدة.

### السيناريوهات:

#### 🧪 اختبار 7.1: Error Boundary
```typescript
// محاكاة خطأ في Component:

// في أي Component للاختبار:
const TestErrorComponent = () => {
  throw new Error("Test error!");
};

// استخدمها داخل ErrorBoundary:
<ErrorBoundary>
  <TestErrorComponent />
</ErrorBoundary>

// يجب أن يظهر:
✅ شاشة الخطأ مع أيقونة ⚠️
✅ رسالة: "عذراً، حدث خطأ!"
✅ زر: "المحاولة مرة أخرى"
✅ في Development Mode: تفاصيل الخطأ
```

#### 🧪 اختبار 7.2: Retry Logic
```typescript
// اختبار إعادة المحاولة عند فشل الشبكة:

// 1. افصل الإنترنت
// 2. حاول تحميل بيانات (مثلاً: قائمة الطلبات)

// يجب أن يظهر في Console:
⚠️ [Retry] Attempt 1/4 failed. Retrying in 1000ms...
⚠️ [Retry] Attempt 2/4 failed. Retrying in 2000ms...
⚠️ [Retry] Attempt 3/4 failed. Retrying in 4000ms...

// 3. أعد الاتصال بالإنترنت قبل المحاولة الأخيرة
✅ [Retry] Succeeded after 3 retries
```

#### 🧪 اختبار 7.3: رسائل الأخطاء المترجمة
```typescript
// اختبر أنواع مختلفة من الأخطاء:

// 1. خطأ شبكة:
fetch('https://invalid-url')
  .catch(err => {
    const message = getErrorMessage(err);
    // المتوقع: "تعذر الاتصال بالخادم. تحقق من اتصال الإنترنت."
  });

// 2. خطأ مصادقة:
// محاولة الوصول بعد انتهاء JWT
// المتوقع: "انتهت صلاحية الجلسة. يرجى تسجيل الدخول مرة أخرى."

// 3. خطأ صلاحيات:
// محاولة حذف بيانات بدون صلاحية
// المتوقع: "ليس لديك صلاحية للقيام بهذا الإجراء."
```

---

## 📍 الاختبار 8: تحديد الموقع GPS

### الهدف:
التحقق من أن التطبيق يحدد موقع العميل تلقائياً.

### الخطوات:

#### 1️⃣ **الإعداد:**
```bash
1. سجل الدخول كعميل
2. افتح الشاشة الرئيسية (Home)
```

#### 2️⃣ **السيناريو الاختباري:**
```typescript
// عند فتح الشاشة:
✅ يجب أن يظهر: "جارٍ تحديد الموقع..."
✅ ثم يظهر طلب إذن الموقع (إذا لم يكن ممنوحاً)
✅ بعد الموافقة: يظهر اسم المدينة والحي
✅ مثال: "الرياض، حي النخيل"
```

#### 3️⃣ **اختبار النقر على الموقع:**
```typescript
// اضغط على شريط الموقع:
✅ يجب أن يبدأ تحديد الموقع مرة أخرى
✅ يظهر: "جارٍ تحديد الموقع..."
✅ يتم التحديث بموقع جديد
```

#### 4️⃣ **اختبار بدون إذن:**
```typescript
// ارفض إذن الموقع:
✅ يجب ظهور Alert: "إذن الموقع مطلوب"
✅ زر: "السماح" لإعادة الطلب
✅ يظهر موقع افتراضي: "الرياض، حي النخيل"
```

#### 5️⃣ **التحقق من Console:**
```javascript
// يجب أن ترى:
📍 Location: { latitude: 24.7136, longitude: 46.6753, accuracy: 5 }
🗺️ Address: { city: "الرياض", district: "حي النخيل", ... }
```

---

## 📊 تقرير نتائج الاختبار

### نموذج تقرير:

```markdown
## 🧪 نتائج الاختبار - [التاريخ]

### ✅ الاختبارات الناجحة:
- [x] Real-time للسائق
- [x] Real-time للتاجر
- [x] Real-time للعميل
- [x] RLS - عزل البيانات
- [x] فحص صورة السائق
- [x] حساب الإيرادات
- [x] Error Boundary
- [x] GPS Location

### ❌ الاختبارات الفاشلة:
(لا يوجد / أو قائمة بالمشاكل)

### ⚠️ ملاحظات:
- ...
```

---

## 🔧 استكشاف الأخطاء

### مشكلة: Real-time لا يعمل
```bash
✓ تحقق من Supabase Dashboard → Settings → API
✓ تأكد من تفعيل Realtime
✓ تحقق من أن الجدول موجود في قائمة Realtime tables
✓ تحقق من RLS policies
```

### مشكلة: RLS يمنع الوصول للبيانات الصحيحة
```sql
-- تحقق من السياسات:
SELECT * FROM pg_policies 
WHERE tablename = 'orders';

-- تحقق من auth.uid():
SELECT auth.uid();
```

### مشكلة: GPS لا يعمل
```bash
✓ تحقق من إذن الموقع في إعدادات الجهاز
✓ تأكد من أن expo-location مثبت
✓ جرب على جهاز حقيقي (Simulator قد لا يعمل جيداً)
```

---

## 🎯 الخلاصة

بعد إكمال جميع الاختبارات:
- ✅ تأكد من توثيق النتائج
- ✅ أصلح أي مشاكل تم اكتشافها
- ✅ أعد الاختبار بعد الإصلاحات
- ✅ انتقل للخطوة التالية (Production أو تحسينات إضافية)

---

**تم إعداد دليل الاختبار بنجاح!** ✅
