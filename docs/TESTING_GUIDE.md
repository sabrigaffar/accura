# 🧪 دليل اختبار النظام الكامل

## 📋 جدول المحتويات
1. [التحضيرات الأساسية](#التحضيرات-الأساسية)
2. [تطبيق قاعدة البيانات](#تطبيق-قاعدة-البيانات)
3. [اختبار نظام التاجر](#اختبار-نظام-التاجر)
4. [اختبار نظام العميل](#اختبار-نظام-العميل)
5. [اختبار نظام السائق](#اختبار-نظام-السائق)
6. [اختبار التكامل الكامل](#اختبار-التكامل-الكامل)
7. [المشاكل الشائعة وحلولها](#المشاكل-الشائعة-وحلولها)

---

## 1️⃣ التحضيرات الأساسية

### **أ. تثبيت المكتبات المطلوبة:**

```bash
# في مجلد المشروع الرئيسي
cd c:\Users\sabri Ga3far\Desktop\project

# تثبيت expo-location
npx expo install expo-location

# التأكد من تثبيت جميع المكتبات
npm install
```

### **ب. التأكد من إعدادات Supabase:**

تحقق من وجود الملف `.env` أو `app.config.js` مع:
```env
EXPO_PUBLIC_SUPABASE_URL=your_supabase_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

---

## 2️⃣ تطبيق قاعدة البيانات

### **أ. تطبيق Migrations الجديدة:**

افتح **Supabase Dashboard** → **SQL Editor** وقم بتشغيل الملفات التالية بالترتيب:

#### **1. خطوات التوصيل:**
```sql
-- supabase/migrations/20251026_add_delivery_steps.sql
ALTER TABLE orders 
ADD COLUMN IF NOT EXISTS picked_up_at TIMESTAMP,
ADD COLUMN IF NOT EXISTS heading_to_merchant_at TIMESTAMP,
ADD COLUMN IF NOT EXISTS heading_to_customer_at TIMESTAMP;

COMMENT ON COLUMN orders.picked_up_at IS 'وقت استلام الطلب من المتجر';
COMMENT ON COLUMN orders.heading_to_merchant_at IS 'وقت بداية التوجه للمتجر';
COMMENT ON COLUMN orders.heading_to_customer_at IS 'وقت بداية التوجه للعميل';

CREATE INDEX IF NOT EXISTS idx_orders_delivery_steps 
ON orders(driver_id, picked_up_at, heading_to_customer_at) 
WHERE driver_id IS NOT NULL;
```

#### **2. جدول إلغاءات السائق:**
```sql
-- supabase/migrations/20251026_add_driver_cancellations.sql
CREATE TABLE IF NOT EXISTS driver_cancellations (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  driver_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  order_id UUID NOT NULL REFERENCES orders(id) ON DELETE CASCADE,
  reason TEXT NOT NULL,
  cancelled_at TIMESTAMP DEFAULT NOW(),
  created_at TIMESTAMP DEFAULT NOW()
);

COMMENT ON TABLE driver_cancellations IS 'سجل إلغاءات الطلبات من طرف السائقين';

CREATE INDEX IF NOT EXISTS idx_driver_cancellations_driver 
ON driver_cancellations(driver_id, cancelled_at DESC);

ALTER TABLE driver_cancellations ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Drivers can view their own cancellations"
ON driver_cancellations FOR SELECT
TO authenticated
USING (driver_id = auth.uid());

CREATE POLICY "Drivers can insert cancellations"
ON driver_cancellations FOR INSERT
TO authenticated
WITH CHECK (driver_id = auth.uid());
```

#### **3. إحداثيات GPS:**
```sql
-- supabase/migrations/20251026_add_gps_coordinates.sql
ALTER TABLE addresses 
ADD COLUMN IF NOT EXISTS latitude DECIMAL(10, 8),
ADD COLUMN IF NOT EXISTS longitude DECIMAL(11, 8);

COMMENT ON COLUMN addresses.latitude IS 'خط العرض GPS';
COMMENT ON COLUMN addresses.longitude IS 'خط الطول GPS';

CREATE INDEX IF NOT EXISTS idx_addresses_coordinates 
ON addresses(latitude, longitude) 
WHERE latitude IS NOT NULL AND longitude IS NOT NULL;
```

### **ب. التحقق من نجاح التطبيق:**

```sql
-- التحقق من الأعمدة الجديدة في orders
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'orders' 
AND column_name IN ('picked_up_at', 'heading_to_merchant_at', 'heading_to_customer_at');

-- التحقق من جدول driver_cancellations
SELECT * FROM information_schema.tables WHERE table_name = 'driver_cancellations';

-- التحقق من الأعمدة في addresses
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'addresses' 
AND column_name IN ('latitude', 'longitude');
```

---

## 3️⃣ اختبار نظام التاجر

### **السيناريو: إنشاء منتج وإدارة طلب**

#### **الخطوات:**

1. **تسجيل الدخول كتاجر:**
   - افتح التطبيق
   - سجل دخول بحساب له دور `merchant`
   - تأكد من ظهور تبويبات التاجر

2. **إضافة منتج جديد:**
   - اذهب لتبويب "المنتجات"
   - اضغط زر "إضافة منتج"
   - املأ البيانات:
     ```
     الاسم: برجر لحم
     الوصف: برجر لذيذ مع الجبن
     السعر: 35 ريال
     الكمية: 50
     الفئة: وجبات سريعة
     ```
   - ارفع صورة (اختياري)
   - احفظ

3. **التحقق من Dashboard:**
   - اذهب لتبويب "الرئيسية"
   - تحقق من:
     - ✅ عدد المنتجات زاد
     - ✅ الإحصائيات تظهر بشكل صحيح
     - ✅ الطلبات الأخيرة (إن وجدت)

4. **إدارة الطلبات:**
   - اذهب لتبويب "الطلبات"
   - إذا كان هناك طلب جديد (`pending`):
     - اضغط على الطلب
     - اضغط "قبول الطلب"
     - غيّر الحالة إلى "جاري التحضير"
     - ثم "جاهز للتوصيل"

### **✅ النتائج المتوقعة:**
- ✅ المنتج يظهر في قائمة المنتجات
- ✅ يمكن تعديل وحذف المنتج
- ✅ الطلبات تتحدث حالتها بنجاح
- ✅ Dashboard يعرض البيانات الصحيحة

---

## 4️⃣ اختبار نظام العميل

### **السيناريو: تصفح المتاجر وإنشاء طلب**

#### **الخطوات:**

1. **تسجيل الدخول كعميل:**
   - افتح التطبيق
   - سجل دخول بحساب له دور `customer`
   - تأكد من ظهور تبويبات العميل

2. **تصفح المتاجر:**
   - اذهب لتبويب "المتاجر"
   - تحقق من:
     - ✅ ظهور قائمة المتاجر
     - ✅ معلومات كل متجر (الاسم، التقييم، رسوم التوصيل)
     - ✅ البحث عن متجر يعمل

3. **فتح متجر ومشاهدة المنتجات:**
   - اضغط على أي متجر
   - تحقق من:
     - ✅ ظهور معلومات المتجر
     - ✅ قائمة المنتجات تظهر
     - ✅ يمكن إضافة منتجات للسلة

4. **إنشاء طلب:**
   - أضف منتجات للسلة
   - اضغط "اذهب للسلة"
   - تحقق من السلة:
     - ✅ المنتجات موجودة
     - ✅ يمكن تعديل الكمية
     - ✅ الإجمالي صحيح
   - اضغط "إتمام الطلب"
   - اختر عنوان التوصيل
   - اختر طريقة الدفع
   - أكّد الطلب

5. **متابعة الطلب:**
   - اذهب لتبويب "طلباتي"
   - تحقق من:
     - ✅ الطلب الجديد يظهر
     - ✅ الحالة صحيحة (`pending`)
     - ✅ تفاصيل الطلب كاملة

### **✅ النتائج المتوقعة:**
- ✅ جميع المتاجر النشطة تظهر
- ✅ يمكن إضافة منتجات للسلة
- ✅ الطلب يُنشأ بنجاح
- ✅ الطلب يظهر للتاجر فوراً

---

## 5️⃣ اختبار نظام السائق (الاختبار الشامل) 🚗

### **السيناريو الكامل: من قبول الطلب حتى التسليم**

#### **أ. التحضيرات:**

1. **تأكد من وجود طلب جاهز:**
   - يجب أن يكون هناك طلب بحالة `ready`
   - إذا لم يكن موجود، استخدم حساب التاجر لتغيير حالة طلب إلى `ready`

2. **تسجيل الدخول كسائق:**
   - افتح التطبيق
   - سجل دخول بحساب له دور `driver`
   - السماح بالوصول للموقع عند الطلب

#### **ب. اختبار الطلبات المتاحة:**

1. **فتح تبويب "الطلبات المتاحة":**
   - تحقق من:
     - ✅ قائمة الطلبات الجاهزة تظهر
     - ✅ معلومات كل طلب واضحة (رقم الطلب، المتجر، العميل، العنوان، الأجرة)
     - ✅ المسافة تظهر (قد تكون رقم عشوائي إذا لم تكن الإحداثيات متوفرة)

2. **اختبار الفلترة والترتيب:**
   - جرّب زر "الأحدث":
     - ✅ الطلبات ترتب حسب التاريخ
   - جرّب زر "الأعلى أجراً":
     - ✅ الطلبات ترتب حسب رسوم التوصيل
   - جرّب زر "الأقرب":
     - ✅ الطلبات ترتب حسب المسافة

3. **قبول طلب:**
   - اختر أي طلب
   - اضغط "قبول الطلب"
   - تأكيد القبول
   - تحقق من:
     - ✅ رسالة نجاح تظهر
     - ✅ انتقال تلقائي لتبويب "طلباتي النشطة"

#### **ج. اختبار الطلب النشط:**

1. **عرض تفاصيل الطلب:**
   - تحقق من ظهور:
     - ✅ رقم الطلب
     - ✅ أجرة التوصيل
     - ✅ عداد خطوات التوصيل (4 خطوات)
     - ✅ معلومات المتجر
     - ✅ معلومات العميل
     - ✅ عنوان التوصيل
     - ✅ تفاصيل الطلب (عدد الأصناف، الإجمالي)

2. **اختبار خطوات التوصيل:**

   **الخطوة 1: بدء التوجه للمتجر**
   - اضغط زر "🚗 بدء التوجه للمتجر"
   - تحقق من:
     - ✅ رسالة نجاح
     - ✅ الخطوة 2 في العداد تصبح نشطة (لون أزرق)
     - ✅ الخط يصبح أخضر

   **الخطوة 2: الاستلام من المتجر**
   - اضغط زر "✅ تم الاستلام من المتجر"
   - تأكيد الاستلام
   - تحقق من:
     - ✅ رسالة "تم الاستلام. الآن يمكنك التوجه للعميل"
     - ✅ الخطوة 3 تصبح نشطة
     - ✅ العداد يتحدث بشكل صحيح

   **الخطوة 3: بدء التوجه للعميل**
   - اضغط زر "🚗 بدء التوجه للعميل"
   - تحقق من:
     - ✅ الخطوة 4 تصبح نشطة
     - ✅ زر "تم التسليم" يصبح نشط

3. **اختبار الاتصال بالعميل:**
   - اضغط زر "اتصال" (بجوار اسم العميل)
   - تحقق من:
     - ✅ تطبيق المكالمات يفتح
     - ✅ رقم العميل صحيح

4. **اختبار التنقل بالخرائط:**
   - اضغط زر "التنقل"
   - تحقق من:
     - ✅ Google Maps يفتح
     - ✅ عنوان التوصيل يظهر

5. **اختبار إلغاء الطلب (اختياري):**
   - اضغط زر "إلغاء الطلب (طارئ)"
   - تحقق من:
     - ✅ نافذة حوار تظهر
     - ✅ حقل إدخال السبب إجباري
   - أدخل سبب: "عطل في السيارة"
   - اضغط "تأكيد الإلغاء"
   - تحقق من:
     - ✅ رسالة نجاح
     - ✅ الطلب يعود للطلبات المتاحة
     - ✅ انتقال لتبويب الطلبات المتاحة

6. **إتمام التوصيل:**
   - بعد إكمال جميع الخطوات
   - اضغط زر "تم التسليم"
   - تأكيد التسليم
   - تحقق من:
     - ✅ رسالة نجاح
     - ✅ تسجيل الأرباح
     - ✅ انتقال لتبويب "أرباحي"

#### **د. اختبار صفحة الأرباح:**

1. **الإحصائيات:**
   - تحقق من:
     - ✅ أرباح اليوم
     - ✅ أرباح الأسبوع
     - ✅ أرباح الشهر
     - ✅ الإجمالي الكلي
     - ✅ عدد التوصيلات

2. **الرسم البياني:**
   - تحقق من:
     - ✅ الرسم البياني يظهر
     - ✅ 7 أعمدة (آخر 7 أيام)
     - ✅ أسماء الأيام بالعربي
     - ✅ الأرقام على الأعمدة

3. **فلترة الأرباح:**
   - جرّب الفلاتر:
     - ✅ "اليوم" - يظهر أرباح اليوم فقط
     - ✅ "هذا الأسبوع" - يظهر أرباح الأسبوع
     - ✅ "هذا الشهر" - يظهر أرباح الشهر
     - ✅ "الكل" - يظهر جميع الأرباح

4. **سجل الأرباح:**
   - تحقق من:
     - ✅ قائمة بجميع التوصيلات
     - ✅ كل توصيلة تحتوي على:
       - رقم الطلب
       - المبلغ
       - اسم العميل
       - التاريخ والوقت

### **✅ النتائج المتوقعة لنظام السائق:**
- ✅ جميع الطلبات الجاهزة تظهر
- ✅ الفلترة والترتيب يعمل بشكل صحيح
- ✅ المسافة تُحسب (إذا كانت الإحداثيات متوفرة)
- ✅ قبول الطلب يعمل
- ✅ خطوات التوصيل تعمل بالتسلسل
- ✅ الاتصال بالعميل يعمل
- ✅ التنقل بالخرائط يعمل
- ✅ إلغاء الطلب يعمل
- ✅ إتمام التوصيل يسجل الأرباح
- ✅ الإحصائيات والرسم البياني يعملان

---

## 6️⃣ اختبار التكامل الكامل

### **السيناريو: رحلة طلب كاملة من البداية للنهاية**

#### **الخطوات:**

1. **العميل:**
   - سجل دخول كعميل
   - اطلب من متجر
   - الطلب يُنشأ بحالة `pending`

2. **التاجر:**
   - سجل دخول كتاجر
   - الطلب يظهر في قائمة الطلبات
   - قبول الطلب → `accepted`
   - بدء التحضير → `preparing`
   - جاهز للتوصيل → `ready`

3. **السائق:**
   - سجل دخول كسائق
   - الطلب يظهر في الطلبات المتاحة
   - قبول الطلب → `out_for_delivery`
   - التوجه للمتجر
   - الاستلام من المتجر
   - التوجه للعميل
   - تم التسليم → `delivered`

4. **التحقق النهائي:**
   - العميل: الطلب يظهر بحالة `delivered`
   - التاجر: الطلب مكتمل
   - السائق: الأرباح مسجلة

### **✅ النتيجة المتوقعة:**
- ✅ التدفق الكامل يعمل بدون أخطاء
- ✅ جميع الحالات تتحدث بشكل صحيح
- ✅ البيانات متسقة عبر جميع الأدوار

---

## 7️⃣ المشاكل الشائعة وحلولها

### **مشكلة: لا تظهر الطلبات المتاحة للسائق**

**الحل:**
```sql
-- تحقق من وجود طلبات بحالة ready
SELECT id, order_number, status, driver_id 
FROM orders 
WHERE status = 'ready' AND driver_id IS NULL;

-- إذا لم توجد، قم بتحديث طلب:
UPDATE orders 
SET status = 'ready', driver_id = NULL 
WHERE id = 'order_id_here';
```

### **مشكلة: خطأ في صلاحيات قاعدة البيانات**

**الحل:**
```sql
-- تحقق من RLS policies
SELECT * FROM pg_policies WHERE tablename = 'orders';

-- إذا لزم الأمر، أعد تطبيق policies من ملف schema.sql
```

### **مشكلة: المسافة لا تظهر بشكل صحيح**

**الأسباب المحتملة:**
1. لم يتم السماح بالوصول للموقع
2. الإحداثيات غير موجودة في جدول addresses
3. مكتبة expo-location غير مثبتة

**الحل:**
```bash
# تثبيت المكتبة
npx expo install expo-location

# إضافة إحداثيات للعناوين يدوياً (للاختبار):
UPDATE addresses 
SET latitude = 24.7136, longitude = 46.6753 
WHERE id = 'address_id_here';
```

### **مشكلة: الرسم البياني لا يظهر**

**الحل:**
- تأكد من وجود أرباح في جدول `driver_earnings`
- تحقق من أن التواريخ صحيحة
- جرّب Pull to refresh

### **مشكلة: لا يمكن الاتصال بالعميل**

**الحل:**
- تأكد من وجود رقم هاتف للعميل في جدول profiles
- تأكد من صلاحيات المكالمات في الجهاز

---

## 📊 Checklist النهائي

### **نظام التاجر:**
- [ ] إضافة منتج
- [ ] تعديل منتج
- [ ] حذف منتج
- [ ] عرض الطلبات
- [ ] قبول طلب
- [ ] تحديث حالة الطلب
- [ ] Dashboard يعمل

### **نظام العميل:**
- [ ] عرض المتاجر
- [ ] فتح متجر
- [ ] إضافة للسلة
- [ ] إنشاء طلب
- [ ] متابعة الطلب

### **نظام السائق:**
- [ ] عرض الطلبات المتاحة
- [ ] فلترة وترتيب الطلبات
- [ ] حساب المسافة
- [ ] قبول طلب
- [ ] خطوات التوصيل (4 خطوات)
- [ ] الاتصال بالعميل
- [ ] التنقل بالخرائط
- [ ] إلغاء الطلب
- [ ] إتمام التوصيل
- [ ] عرض الأرباح
- [ ] الرسم البياني
- [ ] فلترة الأرباح

### **التكامل:**
- [ ] التدفق الكامل: عميل → تاجر → سائق
- [ ] الحالات تتحدث بشكل صحيح
- [ ] البيانات متسقة

---

## 🎉 بعد الاختبار الناجح

إذا نجحت جميع الاختبارات، يكون النظام جاهز للإنتاج! 🚀

**الخطوات التالية:**
1. ✅ إصلاح أي مشاكل وجدتها
2. ✅ إضافة بيانات اختبار أكثر
3. ✅ البدء في نظام الإشعارات
4. ✅ إضافة نظام التقييمات
5. ✅ إطلاق النسخة التجريبية (Beta)

---

## 💡 نصائح مهمة

1. **اختبر على جهاز حقيقي** - خاصة للـ GPS والمكالمات
2. **استخدم أكثر من حساب** - حساب لكل دور
3. **راقب الـ Console** - للتأكد من عدم وجود أخطاء
4. **جرّب السيناريوهات الخاطئة** - مثل إلغاء طلب، حذف منتج، إلخ
5. **تحقق من قاعدة البيانات** - بعد كل عملية

---

**حظاً موفقاً في الاختبار! 🧪✨**

إذا واجهت أي مشكلة، راجع قسم "المشاكل الشائعة" أو اتصل بالدعم الفني.
