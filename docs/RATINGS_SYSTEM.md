# 🌟 نظام التقييمات - دليل شامل

## 📋 نظرة عامة

نظام التقييمات يسمح للعملاء بتقييم التجار والسائقين بعد إتمام الطلب، مما يساعد على تحسين جودة الخدمة وبناء الثقة.

---

## 🎯 الميزات الرئيسية

### **1️⃣ تقييم من العميل**
- تقييم المتجر (1-5 نجوم)
- تقييم السائق (1-5 نجوم)
- إضافة تعليق اختياري
- صفحة تقييم بسيطة وسهلة

### **2️⃣ عرض التقييمات للتاجر**
- متوسط التقييم الإجمالي
- عدد التقييمات الكلي
- توزيع التقييمات (5-1 نجوم)
- قائمة جميع التقييمات مع التفاصيل
- معلومات المراجع والطلب

### **3️⃣ عرض التقييمات للسائق**
- متوسط التقييم الإجمالي
- عدد التقييمات الكلي
- توزيع التقييمات (5-1 نجوم)
- قائمة جميع التقييمات
- تحفيز لتحسين الأداء

---

## 📁 هيكل الملفات

```
app/
├── orders/
│   └── rate-order.tsx              # صفحة تقييم الطلب (للعميل)
│
├── (merchant-tabs)/
│   └── reviews.tsx                 # صفحة التقييمات (للتاجر)
│
└── (driver-tabs)/
    └── reviews.tsx                 # صفحة التقييمات (للسائق)
```

---

## 🗄️ قاعدة البيانات

### جدول `reviews`

```sql
CREATE TABLE reviews (
  id UUID PRIMARY KEY,
  order_id UUID NOT NULL REFERENCES orders(id),
  reviewer_id UUID NOT NULL REFERENCES profiles(id),
  reviewee_id UUID NOT NULL REFERENCES profiles(id),
  reviewee_type TEXT NOT NULL,  -- 'merchant' أو 'driver'
  rating INTEGER NOT NULL CHECK (rating >= 1 AND rating <= 5),
  comment TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### الحقول:
- `order_id`: معرف الطلب
- `reviewer_id`: معرف المُقيّم (العميل)
- `reviewee_id`: معرف المُقيّم (التاجر أو السائق)
- `reviewee_type`: نوع المُقيّم (merchant/driver)
- `rating`: التقييم من 1 إلى 5
- `comment`: التعليق (اختياري)

---

## 🔄 تدفق العمل

### **1. العميل يقيّم بعد التوصيل:**

```
الطلب مُسلّم
    ↓
العميل يفتح صفحة التقييم
    ↓
يختار عدد النجوم للمتجر والسائق
    ↓
يضيف تعليق (اختياري)
    ↓
يضغط "إرسال التقييم"
    ↓
يتم حفظ التقييم في قاعدة البيانات
    ↓
رسالة شكر للعميل
```

### **2. التاجر يرى تقييماته:**

```
التاجر يفتح تبويب "التقييمات"
    ↓
يرى متوسط التقييم والإحصائيات
    ↓
يتصفح جميع التقييمات
    ↓
يرى التعليقات والتواريخ
```

### **3. السائق يرى تقييماته:**

```
السائق يفتح تبويب "تقييماتي"
    ↓
يرى متوسط تقييمه
    ↓
يتصفح التقييمات
    ↓
يستخدم التقييمات لتحسين خدمته
```

---

## 🎨 مكونات الواجهة

### **صفحة التقييم (rate-order.tsx):**

```typescript
// الميزات:
- عرض معلومات الطلب
- نجوم تفاعلية لتقييم المتجر
- نجوم تفاعلية لتقييم السائق
- حقل تعليق لكل تقييم
- زر إرسال مع حالة تحميل
- التحقق من صحة الإدخال
```

### **صفحة عرض التقييمات:**

```typescript
// الميزات:
- بطاقة إحصائيات شاملة
- متوسط التقييم الكبير
- مخطط توزيع النجوم
- قائمة التقييمات
- معلومات المراجع
- التاريخ والطلب
- Pull to refresh
```

---

## 📊 الإحصائيات المعروضة

### **للتاجر والسائق:**

1. **متوسط التقييم**: رقم كبير مع نجوم
2. **عدد التقييمات الكلي**: (X تقييم)
3. **توزيع التقييمات**:
   - 5 نجوم: X تقييم (%)
   - 4 نجوم: X تقييم (%)
   - 3 نجوم: X تقييم (%)
   - 2 نجوم: X تقييم (%)
   - 1 نجمة: X تقييم (%)

---

## 🔗 التكامل مع النظام

### **ربط صفحة التقييم:**

```typescript
// من صفحة الطلب بعد التسليم:
router.push(`/orders/rate-order?orderId=${order.id}`);
```

### **إضافة تبويب التقييمات:**

**للتاجر:**
```typescript
// في (merchant-tabs)/_layout.tsx
<Tabs.Screen
  name="reviews"
  options={{
    title: 'التقييمات',
    tabBarIcon: ({ color }) => <Star size={24} color={color} />,
  }}
/>
```

**للسائق:**
```typescript
// في (driver-tabs)/_layout.tsx
<Tabs.Screen
  name="reviews"
  options={{
    title: 'تقييماتي',
    tabBarIcon: ({ color }) => <Star size={24} color={color} />,
  }}
/>
```

---

## ✅ الميزات المُنفذة

- [x] صفحة تقييم للعميل
- [x] صفحة عرض تقييمات التاجر
- [x] صفحة عرض تقييمات السائق
- [x] إحصائيات شاملة
- [x] توزيع النجوم المرئي
- [x] حالات فارغة
- [x] Pull to refresh
- [x] التحقق من الإدخال

---

## 🔜 الميزات القادمة (اختياري)

- [ ] إمكانية الرد على التقييمات
- [ ] فلترة التقييمات (حسب النجوم)
- [ ] ترتيب التقييمات (الأحدث/الأقدم)
- [ ] الإبلاغ عن تقييم غير لائق
- [ ] تعديل التقييم خلال مدة معينة
- [ ] إشعار عند استلام تقييم جديد

---

## 🎯 نصائح للاستخدام

### **للعملاء:**
- قيّم بصدق لمساعدة الآخرين
- أضف تعليق مفيد وبناء
- التقييم يساعد على تحسين الخدمة

### **للتجار:**
- راجع تقييماتك بانتظام
- تعلم من الملاحظات
- استخدم التقييمات لتحسين خدمتك

### **للسائقين:**
- التقييمات تؤثر على قبول الطلبات
- اهتم بجودة الخدمة
- كن محترفاً ومهذباً

---

## 🎊 النظام جاهز للاستخدام!

نظام التقييمات الآن مكتمل وجاهز للإنتاج. يمكن البدء باستخدامه فوراً!

---

**آخر تحديث:** 2025-10-26
